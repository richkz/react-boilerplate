import React, { Component } from 'react';

import '../styles/layout.css';

class Home extends Component {
    render() {
        return(
            <section id="page-home" className="container">Home Page</section>
        );
    }
}

export default Home;